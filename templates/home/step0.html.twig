{% extends "site.html.twig" %}
{% block title 'Page d\'accueil' %}

{% block body %}
    <section id="hero" class="d-flex flex-column justify-content-center">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-xl-8">
                    <h1>Text d'introduction</h1>
                    <h2>petite description : ...</h2>
                    {{ form_start(form) }}
                        {{ form_row(form.language, {attr: {'class': 'form-select form-select-sm'}}) }}

                        <div class="row my-5">
                            <div class="col-12 text-center">
                                <a href="https://www.youtube.com/watch?v=pXJ7ZxhXWy4&ab_channel=4KVideoNature-FocusMusicQ" class="glightbox play-btn mb-4"></a>
                            </div>
                        </div>
                        <div class="text-right my-4">
                            <button class="btn btn-success" type="submit">
                                <i class="far fa-check-circle"></i>
                                {{ 'homepage.action.next'|trans }}
                            </button>
                        </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </section><!-- End Hero -->
{% endblock %}
